# EPIC-006: EN-центричная документация

- **Статус:** done (2026-07-09)
- **Создан:** 2026-07-08
- **Roadmap:** не требуется

## Цель

Сделать пользовательскую документацию англоязычной (основной вариант), сохранив русские
версии как альтернативу (`*.ru.md`, по образцу README/README.ru.md). Расширяет аудиторию
библиотеки: PyPI, международные пользователи, цитируемость.

## Контекст

Запрос от 2026-07-08 (после EN-центричного README). Внутренние dev-документы (PROJECT,
INFRASTRUCTURE, DOD, epics, roadmaps, gap-анализ) остаются на русском — это рабочий язык
проекта. Конвенция: `docs/<name>.md` — EN (primary), `docs/<name>.ru.md` — RU (альтернатива,
поддерживается best effort; EN обязателен).

## Задачи

- [x] Перевести пользовательские доки (EN primary + RU-копия): README, getting_started,
      concepts, tesaurus, models, distributions, simulation, calculation, custom_simulators,
      examples, networks, priorities, srpt_spjf_methods, negative_* — 16 доков,
      4 параллельных агента (2026-07-08)
- [x] Переключатели языка в шапках обеих версий; ссылки: EN→EN, RU→RU (где есть)
- [x] Корневые README обновлены (README.ru.md → .ru.md-доки); тезаурус добавлен в индекс
- [x] Языковая политика зафиксирована в PROJECT.md и DoD
- [x] Проверка ссылок скриптом: 0 битых относительных ссылок в docs/
- [x] **EN-версии всех схем каталога** (2026-07-09): `generate_figures.py` параметризован по
      языку хелпером `t(en, ru)`, рендерит `<name>.png` (EN) + `<name>.ru.png` (RU);
      EN-каталоги используют `.png`, RU-каталоги — `.ru.png`. 40 схем.

## Критерии готовности (DoD эпика)

Все пользовательские доки доступны на EN как основной вариант; RU-версии сохранены без потерь;
битых ссылок нет; политика задокументирована.

## Результаты

- 16 пользовательских доков переведены (EN primary + RU-копия) с языковыми переключателями.
- Все 20 схем каталога двуязычны (EN `<name>.png`, RU `<name>.ru.png`); генератор
  параметризован по языку.
- Разделы PH/MAP добавлены в справочник распределений (EN+RU) с объяснением матрицы T
  и схемами `ph_gallery`/`mmpp` — по запросу пользователя.
- Языковая политика: EN — основной, RU — альтернатива; dev-документы (эпики, roadmaps,
  PROJECT/DOD/INFRASTRUCTURE, gap-анализ) остаются на русском.
